name "drmi"

targetType "library"
description "D Remote Method Invocation help wraps"
authors "Oleg Butko (deviator)"
copyright "Copyright © 2017 Oleg Butko"
license "MIT"

subPackage {
    name "mqtt"

    description "mqtt rmi"

    sourcePaths "mqtt"
    importPaths "mqtt"

    dependency "drmi" path="./drmi"

    configuration "rtload" {
        targetType "library"
    }

    configuration "ctlink" {
        targetType "library"
        libs "mosquitto"
    }
}
